/* Generated by Pak Compiler */

#include <libdragon.h>
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>
#include "pak_math.h"
#include "pak_containers.h"


/* -- Pak runtime types -- */
typedef struct { const char *data; int32_t len; } PakStr;
typedef struct { uint8_t *base; uint8_t *ptr; size_t capacity; } PakArena;
static inline PakStr pak_str_from_cstr(const char *s) {
    return (PakStr){ .data = s, .len = (int32_t)strlen(s) }; }
static inline bool pak_str_eq(PakStr a, PakStr b) {
    return a.len == b.len && memcmp(a.data, b.data, (size_t)a.len) == 0; }
static inline void *pak_arena_alloc(PakArena *a, size_t sz) {
    sz = (sz + 7) & ~(size_t)7;  /* 8-byte align */
    if (a->ptr + sz > a->base + a->capacity) return NULL;
    void *p = a->ptr; a->ptr += sz; return p; }
static inline void pak_arena_reset(PakArena *a) { a->ptr = a->base; }

typedef struct { bool is_ok; union { int32_t value; LoadError error; } data; } PakResult_int32_t_LoadError;
typedef enum {
    LoadError_file_not_found,
    LoadError_bad_format,
    LoadError_out_of_memory,
} LoadError;

PakResult_int32_t_LoadError load_data(char * path) {
    if (path == NULL) {
        return (PakResult_int32_t_LoadError){ .is_ok = false, .data.error = LoadError_file_not_found };
    }
    return (PakResult_int32_t_LoadError){ .is_ok = true, .data.value = 42 };
}

PakResult_int32_t_LoadError divide(int32_t a, int32_t b) {
    if (b == 0) {
        return (PakResult_int32_t_LoadError){ .is_ok = false, .data.error = LoadError_bad_format };
    }
    return (PakResult_int32_t_LoadError){ .is_ok = true, .data.value = (a / b) };
}

static int32_t sink = 0;

int main(void) {
    __auto_type r = load_data("level.bin");
    switch (r) {
        case /* ok(data) */:
        {
            sink = data;
            break;
        }
        case /* err(e) */:
        {
            switch (e) {
                case LoadError_file_not_found:
                {
                    sink = -1;
                    break;
                }
                case LoadError_bad_format:
                {
                    sink = -2;
                    break;
                }
                case LoadError_out_of_memory:
                {
                    sink = -3;
                    break;
                }
            }
            break;
        }
    }
    __auto_type d = divide(10, 2);
    switch (d) {
        case /* err(e) */:
        {
            sink = -10;
            break;
        }
        case /* ok(v) */:
        {
            sink = v;
            break;
        }
    }
    PakResult_int32_t_LoadError always_ok = (PakResult_int32_t_LoadError){ .is_ok = true, .data.value = 100 };
    PakResult_int32_t_LoadError always_err = (PakResult_int32_t_LoadError){ .is_ok = false, .data.error = LoadError_bad_format };
    switch (always_ok) {
        case /* ok(v) */:
        {
            sink = v;
            break;
        }
        case /* err(e) */:
        {
            break;
        }
    }
    switch (always_err) {
        case /* ok(v) */:
        {
            break;
        }
        case /* err(e) */:
        {
            sink = -1;
            break;
        }
    }
    return 0;
}

